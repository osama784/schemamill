{
  "name": "@schemamill/postgres",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PostgreSQL support for schemamill: introspection and DDL rendering.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": "^22.22.3 || ^24.15.0 || >=26.0.0"
  },
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint .",
    "typecheck": "tsc -b",
    "test": "pnpm run build && node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@schemamill/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
