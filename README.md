# schemamill (working name)

> A local-first PostgreSQL modeling studio: design your schema as a canonical model, import DDL or introspect a live database, round-trip edits, and generate reviewable, hazard-annotated migration SQL.

**Status:** Phase A — core modeling & distillation. Nothing is usable yet.

**License:** MIT
